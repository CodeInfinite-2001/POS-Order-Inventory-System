# ===============================================
# POS Order & Inventory System - Environment Config
# ===============================================

# Node Environment ('production' or 'development')
NODE_ENV=production

# Server Port (Railway automatically sets PORT at runtime)
PORT=5000

# MongoDB Database Connection
# In Railway: Railway automatically provides MONGO_URL when you add MongoDB service!
# Supported variables (in order of priority):
# - MONGODB_URI
# - MONGO_URL
# - MONGO_PRIVATE_URL
# - MONGODB_URL
# - DATABASE_URL
MONGODB_URI=mongodb://localhost:27017/pos_inventory

# JWT Secret for Session & Bearer Authentication
JWT_SECRET=pos-concurrency-safe-secret-key-987654321

# Concurrency & Order Expiry Settings
RESERVATION_DURATION_SEC=300
EXPIRY_WORKER_INTERVAL_MS=5000

# Client Configuration (Only needed if Frontend is hosted separately from Backend)
# If unified, leave empty or unset:
# VITE_API_URL=https://your-backend.up.railway.app
